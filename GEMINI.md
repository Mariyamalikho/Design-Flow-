# GitHub Workflow Constraints
- **Issue Linking:** Always use the exact keyword "Fixes #<issue-number>" (do not use "Resolves", "Closes", etc.) when linking issues in PR bodies.
- **Naming Conventions:** Never use "Day X" or sprint numbers in branch names, Pull Request titles, or Issue titles.
